version: "3.7"

services:
  ImgWebPy:
    image: ghcr.io/buttercheetah/gamekit:latest
    restart: unless-stopped
    environment:
      - STEAM_KEY=<changeme>
      - WEB_URL=<changeme>
      - TIMEZONE=UTC
    ports:
      - 9996:5000